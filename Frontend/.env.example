# URL of the backend or active ngrok tunnel. Replace this when the tunnel changes.
VITE_API_BASE_URL=https://cultivate-suitable-manmade.ngrok-free.dev

# In development, route API requests through Vite to prevent browser CORS errors.
VITE_USE_DEV_PROXY=true
